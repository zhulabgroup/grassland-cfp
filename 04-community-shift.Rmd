# Community shift analyses

Let us begin with the theory of moment method estimation, which is a generalization of the CTI/CPI approach. For location $i$, species $j$, denote species abundance (point intercept or percent cover) as $A_{ij}$, and species trait (e.g., optimal temperature) as $T_j$. At the community level, the expected trait (i.e., first moment), which is equivalent to CTI/CPI, is

$$
\mathrm{E}(T_i) = \bar{T_i} = \frac{\sum_j A_{ij}T_j}{\sum_j A_{ij}}.
$$

Furthermore, the trait variance (i.e., second central moment), using the variance-expectation formula, is

$$
\mathrm{Var}(T_i) = \mathrm{E}[T_i^2] - \mathrm{E}[T_i]^2 = \frac{\sum_j A_{ij}T_j^2}{\sum_j A_{ij}} - \bar{T_i}^2.
$$

A pair of $\{\mathrm{E}(T_i), \mathrm{Var}(T_i)\}$ characterizes the trait values under the distribution of species abundance. Operationally, we might use a pair of mean and standard deviation, $\{ \mu(T_i) = \mathrm{E}(T_i), \sigma(T_i) = \sqrt{\mathrm{Var}(T_i)}\}$, so that they are comparable with the same unit. 

Setup and load packages.

```{r, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, eval = TRUE)
```

```{r}
library(tidyverse)
library(sf)
```

Show sampling year for plot by site for experimental and observational data.

```{r}
tidy_exp_tbl %>%
  distinct(site, plot, year) %>%
  ggplot(aes(year, plot)) +
  geom_point() +
  facet_wrap(~site, scales = "free")

tidy_obs_tbl %>%
  distinct(site, plot, year) %>%
  ggplot(aes(year, plot)) +
  geom_point() +
  facet_wrap(~site, scales = "free")
```

Load niche estimates.

```{r}
niche_file <- "data/occurrence/niche-estimates-cfp-2022-06-13.rds"
niche_tbl <- read_rds(niche_file)
```

## Experimental data

Load all experimental community data. Join species climate niche estimates with community data, then aggregate by site, year, plot, treatment, calculate community-averaged temperature and precipitation, weighted by abundance.

```{r}
exp_com_tbl <- read_rds("data/community/all-experimental-data.rds") %>%
  inner_join(niche_tbl, by = "species") %>%
  group_by(site, year, plot, treat) %>%
  summarize(
    tmp_com_mean = sum(abund * tmp_occ_mean) / sum(abund),
    tmp_com_var = sum(abund * tmp_occ_mean^2) / sum(abund) - tmp_com_mean^2,
    ppt_com_mean = sum(abund * ppt_occ_mean) / sum(abund),
    ppt_com_var = sum(abund * ppt_occ_mean^2) / sum(abund) - ppt_com_mean^2,
    vpd_com_mean = sum(abund * vpd_occ_mean) / sum(abund),
    vpd_com_var = sum(abund * vpd_occ_mean^2) / sum(abund) - vpd_com_mean^2
  )
```

### JRGCE 

#### CTI ~ warming treatment

Community temperature niches responding to warming treatments.

Community-level mean CTI in ambient vs. warming plots, controlling other treatments.

```{r, fig.cap="Compare mean(CTI) in ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "T___")
  ) %>%
  ggplot(aes(year, tmp_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level mean CTI in all 64 ambient vs. warming plots (pooling together all the other P, C, N treatments).

```{r, fig.cap="Compare mean(CTI) in all 64 ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_T = str_sub(treat, start = 1L, end = 1L)) %>%
  ggplot(aes(year, tmp_com_mean, col = treat_T, group = interaction(treat_T, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_T),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CTI standard deviation in ambient vs. warming plots.

```{r, fig.cap="Compare sd(CTI) in ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "T___")
  ) %>%
  ggplot(aes(year, sqrt(tmp_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CTI standard deviation in all 64 ambient vs. warming plots (pooling together all the other P, C, N treatments).

```{r, fig.cap="Compare sd(CTI) in all 64 ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_T = str_sub(treat, start = 1L, end = 1L)) %>%
  ggplot(aes(year, sqrt(tmp_com_var), col = treat_T, group = interaction(treat_T, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_T),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

#### CPI ~ warming treatment

Community precipitation niches responding to warming treatments.

Community-level mean CPI in ambient vs. warming plots, controlling other treatments.

```{r, fig.cap="Compare mean(CPI) in ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "T___")
  ) %>%
  ggplot(aes(year, ppt_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level mean CPI in all 64 ambient vs. warming plots (pooling together all the other P, C, N treatments).

```{r, fig.cap="Compare mean(CPI) in all 64 ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_T = str_sub(treat, start = 1L, end = 1L)) %>%
  ggplot(aes(year, ppt_com_mean, col = treat_T, group = interaction(treat_T, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_T),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CPI standard deviation in ambient vs. warming plots.

```{r, fig.cap="Compare sd(CPI) in ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "T___")
  ) %>%
  ggplot(aes(year, sqrt(ppt_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CPI standard deviation in all 64 ambient vs. warming plots (pooling together all the other P, C, N treatments).

```{r, fig.cap="Compare sd(CPI) in all 64 ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_T = str_sub(treat, start = 1L, end = 1L)) %>%
  ggplot(aes(year, sqrt(ppt_com_var), col = treat_T, group = interaction(treat_T, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_T),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

#### CVI ~ warming treatment

Community VPDmax niches responding to warming treatments.

Community-level mean CVI in ambient vs. warming plots, controlling other treatments.

```{r, fig.cap="Compare mean(CVI) in ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "T___")
  ) %>%
  ggplot(aes(year, vpd_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level mean CVI in all 64 ambient vs. warming plots (pooling together all the other P, C, N treatments).

```{r, fig.cap="Compare mean(CVI) in all 64 ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_T = str_sub(treat, start = 1L, end = 1L)) %>%
  ggplot(aes(year, vpd_com_mean, col = treat_T, group = interaction(treat_T, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_T),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CVI standard deviation in ambient vs. warming plots.

```{r, fig.cap="Compare sd(CVI) in ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "T___")
  ) %>%
  ggplot(aes(year, sqrt(vpd_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CVI standard deviation in all 64 ambient vs. warming plots (pooling together all the other P, C, N treatments).

```{r, fig.cap="Compare sd(CVI) in all 64 ambient vs. warming treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_T = str_sub(treat, start = 1L, end = 1L)) %>%
  ggplot(aes(year, sqrt(vpd_com_var), col = treat_T, group = interaction(treat_T, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_T),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Warming treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

#### CTI ~ watering treatment

Community temperature niches responding to watering treatments.

Community-level mean CTI in ambient vs. watering plots.

```{r, fig.cap="Compare mean(CTI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "_P__")
  ) %>%
  ggplot(aes(year, tmp_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level mean CTI in all 64 ambient vs. watering plots (pooling together all the other T, C, N treatments).

```{r, fig.cap="Compare mean(CTI) in all 64 ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_P = str_sub(treat, start = 2L, end = 2L)) %>%
  ggplot(aes(year, tmp_com_mean, col = treat_P, group = interaction(treat_P, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_P),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level CTI standard deviation in ambient vs. watering plots.

```{r, fig.cap="Compare sd(CTI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "_P__")
  ) %>%
  ggplot(aes(year, sqrt(tmp_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level CTI standard deviation in all 64 ambient vs. watering plots (pooling together all the other T, C, N treatments).

```{r, fig.cap="Compare sd(CTI) in all 64 ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_P = str_sub(treat, start = 2L, end = 2L)) %>%
  ggplot(aes(year, sqrt(tmp_com_var), col = treat_P, group = interaction(treat_P, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_P),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

#### CPI ~ watering treatment

Community precipitation niches responding to watering treatments.

Community-level mean CPI in ambient vs. watering plots.

```{r, fig.cap="Compare mean(CPI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "_P__")
  ) %>%
  ggplot(aes(year, ppt_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level mean CPI in all 64 ambient vs. watering plots (pooling together all the other T, C, N treatments).

```{r, fig.cap="Compare mean(CPI) in all 64 ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_P = str_sub(treat, start = 2L, end = 2L)) %>%
  ggplot(aes(year, ppt_com_mean, col = treat_P, group = interaction(treat_P, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_P),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level CPI standard deviation in ambient vs. watering plots.

```{r, fig.cap="Compare sd(CPI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "_P__")
  ) %>%
  ggplot(aes(year, sqrt(ppt_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level CPI standard deviation in all 64 ambient vs. watering plots (pooling together all the other T, C, N treatments).

```{r, fig.cap="Compare sd(CPI) in all 64 ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_P = str_sub(treat, start = 2L, end = 2L)) %>%
  ggplot(aes(year, sqrt(ppt_com_var), col = treat_P, group = interaction(treat_P, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_P),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

#### CVI ~ watering treatment

Community VPDmax niches responding to watering treatments.

Community-level mean CVI in ambient vs. watering plots.

```{r, fig.cap="Compare mean(CVI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "_P__")
  ) %>%
  ggplot(aes(year, vpd_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level mean CVI in all 64 ambient vs. watering plots (pooling together all the other T, C, N treatments).

```{r, fig.cap="Compare mean(CVI) in all 64 ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_P = str_sub(treat, start = 2L, end = 2L)) %>%
  ggplot(aes(year, vpd_com_mean, col = treat_P, group = interaction(treat_P, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_P),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level CVI standard deviation in ambient vs. watering plots.

```{r, fig.cap="Compare sd(CVI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "_P__")
  ) %>%
  ggplot(aes(year, sqrt(vpd_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level CVI standard deviation in all 64 ambient vs. watering plots (pooling together all the other T, C, N treatments).

```{r, fig.cap="Compare sd(CVI) in all 64 ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "jrgce") %>%
  mutate(treat_P = str_sub(treat, start = 2L, end = 2L)) %>%
  ggplot(aes(year, sqrt(vpd_com_var), col = treat_P, group = interaction(treat_P, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat_P),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

#### Joint analyses

For both warming and watering treatments, show replicates as raw data.

```{r, fig.cap="Plot (replicate) level community weighted temperature and precipitation indexes from ambient and elevated temp and precip treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "T___", "_P__", "TP__")
  ) %>%
  mutate(year = as.factor(year)) %>%
  ggplot(aes(tmp_com_mean, ppt_com_mean, group = interaction(treat, year))) +
  geom_point(aes(color = year), alpha = .5) +
  # stat_ellipse() +
  facet_wrap(~treat) +
  labs(
    x = "Community Temperature Index (CTI, °C)",
    y = "Community Precipitation Index (CPI, mm)",
    col = "Year"
  ) +
  scale_color_viridis_d()
```

Because of outliers, summarize by median (same as boxplot, not mean) values of replicates from the same treatment.

```{r, fig.cap="Median community weighted temperature and precipitation indexes from ambient and elevated temp and precip treatments over years"}
exp_com_tbl %>%
  filter(
    site == "jrgce",
    treat %in% c("____", "T___", "_P__", "TP__")
  ) %>%
  group_by(year, treat) %>%
  summarize(tmp_com_mean = median(tmp_com_mean), ppt_com_mean = median(ppt_com_mean)) %>%
  # mutate(year = as.factor(year)) %>%
  ggplot(aes(tmp_com_mean, ppt_com_mean, col = year)) +
  geom_path() +
  facet_wrap(~treat) +
  labs(
    x = "Community Temperature Index (CTI, °C)",
    y = "Community Precipitation Index (CPI, mm)",
    col = "Year"
  ) +
  scale_color_viridis_c()
```

### McLaughlin Water Experiment

#### CTI ~ watering treatment

Community temperature niches responding to watering treatments.

Community-level mean CTI in ambient vs. watering plots, in non-serpentine and serpentine soils.

```{r, fig.cap="Compare mean(CTI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("_X", "WX")) %>%
  ggplot(aes(year, tmp_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level CTI standard deviation in ambient vs. watering plots, in non-serpentine and serpentine soils.

```{r, fig.cap="Compare sd(CTI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("_X", "WX")) %>%
  ggplot(aes(year, sqrt(tmp_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

#### CPI ~ watering treatment

Community precipitation niches responding to watering treatments.

Community-level mean CPI in ambient vs. watering plots, in non-serpentine and serpentine soils.

```{r, fig.cap="Compare mean(CPI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("_X", "WX")) %>%
  ggplot(aes(year, ppt_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level CPI standard deviation in ambient vs. watering plots, in non-serpentine and serpentine soils.

```{r, fig.cap="Compare sd(CPI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("_X", "WX")) %>%
  ggplot(aes(year, sqrt(ppt_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

#### CVI ~ watering treatment

Community VPDmax niches responding to watering treatments.

Community-level mean CVI in ambient vs. watering plots, in non-serpentine and serpentine soils.

```{r, fig.cap="Compare mean(CVI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("_X", "WX")) %>%
  ggplot(aes(year, vpd_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

Community-level CVI standard deviation in ambient vs. watering plots, in non-serpentine and serpentine soils.

```{r, fig.cap="Compare sd(CVI) in ambient vs. watering treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("_X", "WX")) %>%
  ggplot(aes(year, sqrt(vpd_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Watering treatment"
  ) +
  scale_color_manual(values = c("black", "blue"))
```

#### CTI ~ drought treatment

Community temperature niches responding to drought (shelter) treatments.

Community-level mean CTI in ambient vs. drought plots, in serpentine soil.

```{r, fig.cap="Compare mean(CTI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("X_", "XD")) %>%
  ggplot(aes(year, tmp_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CTI standard deviation in ambient vs. drought plots, in serpentine soil.

```{r, fig.cap="Compare sd(CTI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("X_", "XD")) %>%
  ggplot(aes(year, sqrt(tmp_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

#### CPI ~ drought treatment

Community precipitation niches responding to drought (shelter) treatments.

Community-level mean CPI in ambient vs. drought plots, in serpentine soil.

```{r, fig.cap="Compare mean(CPI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("X_", "XD")) %>%
  ggplot(aes(year, ppt_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CPI standard deviation in ambient vs. drought plots, in serpentine soil.

```{r, fig.cap="Compare sd(CPI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("X_", "XD")) %>%
  ggplot(aes(year, sqrt(ppt_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

#### CVI ~ drought treatment

Community VPDmax niches responding to drought (shelter) treatments.

Community-level mean CVI in ambient vs. drought plots, in serpentine soil.

```{r, fig.cap="Compare mean(CVI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("X_", "XD")) %>%
  ggplot(aes(year, vpd_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CVI standard deviation in ambient vs. drought plots, in serpentine soil.

```{r, fig.cap="Compare sd(CVI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site == "mclexp") %>%
  separate(treat, c("treat", "soil"), sep = 2) %>%
  mutate(
    soil = case_when(
      soil == "S" ~ "Serpentine soil",
      soil == "N" ~ "Non-serpentine soil"
    )
  ) %>%
  filter(treat %in% c("X_", "XD")) %>%
  ggplot(aes(year, sqrt(vpd_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~soil) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

### Santa Cruz IDE

Michael said Marshall Field has lots of native species, while Arboretum and YLR are highly invaded (2/16/22). That might explain the difference among those sites. 

#### CTI ~ drought treatment

Community temperature niches responding to drought treatments.

Community-level mean CTI in ambient vs. drought plots.

```{r, fig.cap="Compare mean(CTI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site %in% c("scide_arboretum", "scide_marshallfield", "scide_ylr")) %>%
  ggplot(aes(year, tmp_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~site) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CTI standard deviation in ambient vs. drought plots.

```{r, fig.cap="Compare sd(CTI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site %in% c("scide_arboretum", "scide_marshallfield", "scide_ylr")) %>%
  ggplot(aes(year, sqrt(tmp_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~site) +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

#### CPI ~ drought treatment

Community precipitation niches responding to drought treatments.

Community-level mean CPI in ambient vs. drought plots.

```{r, fig.cap="Compare mean(CPI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site %in% c("scide_arboretum", "scide_marshallfield", "scide_ylr")) %>%
  ggplot(aes(year, ppt_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~site) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CPI standard deviation in ambient vs. drought plots.

```{r, fig.cap="Compare sd(CPI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site %in% c("scide_arboretum", "scide_marshallfield", "scide_ylr")) %>%
  ggplot(aes(year, sqrt(ppt_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~site) +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

#### CVI ~ drought treatment

Community VPDmax niches responding to drought treatments.

Community-level mean CVI in ambient vs. drought plots.

```{r, fig.cap="Compare mean(CVI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site %in% c("scide_arboretum", "scide_marshallfield", "scide_ylr")) %>%
  ggplot(aes(year, vpd_com_mean, col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~site) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

Community-level CVI standard deviation in ambient vs. drought plots.

```{r, fig.cap="Compare sd(CVI) in ambient vs. drought treatments over years"}
exp_com_tbl %>%
  filter(site %in% c("scide_arboretum", "scide_marshallfield", "scide_ylr")) %>%
  ggplot(aes(year, sqrt(vpd_com_var), col = treat, group = interaction(treat, year))) +
  geom_boxplot() +
  ggpubr::stat_compare_means(
    aes(group = treat),
    method = "wilcox.test",
    label = "p.signif", hide.ns = FALSE
  ) +
  facet_wrap(~site) +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)",
    color = "Drought treatment"
  ) +
  scale_color_manual(values = c("black", "red"))
```

## Observational data

Load all experimental community data. Join species climate niche estimates with community data, then aggregate by site, year, plot, and calculate community-averaged temperature and precipitation, weighted by abundance.

```{r}
obs_com_tbl <- read_rds("data/community/all-observational-data.rds") %>%
  inner_join(niche_tbl, by = "species") %>%
  group_by(site, year, plot) %>%
  summarize(
    tmp_com_mean = sum(abund * tmp_occ_mean) / sum(abund),
    tmp_com_var = sum(abund * tmp_occ_mean^2) / sum(abund) - tmp_com_mean^2,
    ppt_com_mean = sum(abund * ppt_occ_mean) / sum(abund),
    ppt_com_var = sum(abund * ppt_occ_mean^2) / sum(abund) - ppt_com_mean^2,
    vpd_com_mean = sum(abund * vpd_occ_mean) / sum(abund),
    vpd_com_var = sum(abund * vpd_occ_mean^2) / sum(abund) - vpd_com_mean^2
  )
```

### Community change

#### CTI ~ year

Community temperature niches change over years.

CTI community-level mean in several sites increases over years, suggesting communities are becoming "warmer," i.e., thermophilization.

```{r, fig.cap="Compare mean(CTI) across sites over years"}
obs_com_tbl %>%
  ggplot(aes(year, tmp_com_mean, group = year)) +
  geom_boxplot() +
  geom_smooth(
    method = "lm", se = FALSE,
    color = "red", lty = "dashed",
    aes(group = 1)
  ) +
  ggpubr::stat_cor(aes(group = 1, label = ..p.label..),
    p.accuracy = 0.05,
    color = "red"
  ) +
  facet_wrap(~site, scales = "free_y") +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)"
  )
```

CTI community-level standard deviation.

```{r, fig.cap="Compare sd(CTI) across sites over years"}
obs_com_tbl %>%
  ggplot(aes(year, sqrt(tmp_com_var), group = year)) +
  geom_boxplot() +
  geom_smooth(
    method = "lm", se = FALSE,
    color = "red", lty = "dashed",
    aes(group = 1)
  ) +
  ggpubr::stat_cor(aes(group = 1, label = ..p.label..),
    p.accuracy = 0.05,
    color = "red"
  ) +
  facet_wrap(~site, scales = "free_y") +
  labs(
    x = "Year", y = "Community Temperature Index (CTI, °C)"
  )
```

#### CPI ~ year

Community precipitation niches change over years.

CPI community-level mean in several sites decreases over years, suggesting communities are becoming "drier," i.e., mesophilization.

```{r, fig.cap="Compare mean(CPI) across sites over years"}
obs_com_tbl %>%
  ggplot(aes(year, ppt_com_mean, group = year)) +
  geom_boxplot() +
  geom_smooth(
    method = "lm", se = FALSE,
    color = "red", lty = "dashed",
    aes(group = 1)
  ) +
  ggpubr::stat_cor(aes(group = 1, label = ..p.label..),
    p.accuracy = 0.05,
    color = "red"
  ) +
  facet_wrap(~site, scales = "free_y") +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)"
  )
```

CPI community-level standard deviation.

```{r, fig.cap="Compare sd(CPI) across sites over years"}
obs_com_tbl %>%
  ggplot(aes(year, sqrt(ppt_com_var), group = year)) +
  geom_boxplot() +
  geom_smooth(
    method = "lm", se = FALSE,
    color = "red", lty = "dashed",
    aes(group = 1)
  ) +
  ggpubr::stat_cor(aes(group = 1, label = ..p.label..),
    p.accuracy = 0.05,
    color = "red"
  ) +
  facet_wrap(~site, scales = "free_y") +
  labs(
    x = "Year", y = "Community Precipitation Index (CPI, mm)"
  )
```

#### CVI ~ year

Community VPDmax niches change over years.

CVI community-level mean in several sites changes over years, but no clear direction.

```{r, fig.cap="Compare mean(CVI) across sites over years"}
obs_com_tbl %>%
  ggplot(aes(year, vpd_com_mean, group = year)) +
  geom_boxplot() +
  geom_smooth(
    method = "lm", se = FALSE,
    color = "red", lty = "dashed",
    aes(group = 1)
  ) +
  ggpubr::stat_cor(aes(group = 1, label = ..p.label..),
    p.accuracy = 0.05,
    color = "red"
  ) +
  facet_wrap(~site, scales = "free_y") +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)"
  )
```

CPI community-level standard deviation.

```{r, fig.cap="Compare sd(CVI) across sites over years"}
obs_com_tbl %>%
  ggplot(aes(year, sqrt(vpd_com_var), group = year)) +
  geom_boxplot() +
  geom_smooth(
    method = "lm", se = FALSE,
    color = "red", lty = "dashed",
    aes(group = 1)
  ) +
  ggpubr::stat_cor(aes(group = 1, label = ..p.label..),
    p.accuracy = 0.05,
    color = "red"
  ) +
  facet_wrap(~site, scales = "free_y") +
  labs(
    x = "Year", y = "Community VPDmax Index (CVI, Pa)"
  )
```

### Climate change

Observational site climate change over the study period.

```{r}
site_tbl <- read_rds("data/community/site-info.rds")
```

Download PRISM data (to be updated with Chelsa). Set PRISM dir first.

```{r}
prism::prism_set_dl_dir("data/climate/prism/")
```

Download (not run, only once).

```{r, eval=FALSE}
range(exp_com_tbl$year) # 1998:2021
range(obs_com_tbl$year) # 1983:2019
prism::get_prism_annual("tmean", years = 1983:2021, keepZip = FALSE)
prism::get_prism_annual("ppt", years = 1983:2021, keepZip = FALSE)
prism::get_prism_annual("vpdmax", years = 1983:2021, keepZip = FALSE)
```

Extract site climate from PRISM.

```{r}
site_tmp_tbl <- prism::prism_archive_subset(
  "tmean", "annual",
  years = 1983:2020
) %>%
  prism::pd_stack() %>%
  raster::extract(site_tbl) %>%
  bind_cols(site = site_tbl$abbr, .) %>%
  pivot_longer(-site, names_to = "year", values_to = "tmp") %>%
  mutate(year = year %>%
    str_sub(start = -8L, end = -5L) %>%
    as.integer())

site_ppt_tbl <- prism::prism_archive_subset(
  "ppt", "annual",
  years = 1983:2020
) %>%
  prism::pd_stack() %>%
  raster::extract(site_tbl) %>%
  bind_cols(site = site_tbl$abbr, .) %>%
  pivot_longer(-site, names_to = "year", values_to = "ppt") %>%
  mutate(year = year %>%
    str_sub(start = -8L, end = -5L) %>%
    as.integer())

site_vpd_tbl <- prism::prism_archive_subset(
  "vpdmax", "annual",
  years = 1983:2020
) %>%
  prism::pd_stack() %>%
  raster::extract(site_tbl) %>%
  bind_cols(site = site_tbl$abbr, .) %>%
  pivot_longer(-site, names_to = "year", values_to = "vpd") %>%
  mutate(year = year %>%
    str_sub(start = -8L, end = -5L) %>%
    as.integer())
```

Assemble site climate data.

```{r}
site_clim_tbl <- site_tmp_tbl %>%
  full_join(site_ppt_tbl, by = c("site", "year")) %>%
  full_join(site_vpd_tbl, by = c("site", "year")) %>%
  right_join(obs_com_tbl %>%
    distinct(site, year), by = c("site", "year")) %>%
  arrange(site, year)
```

Plot temperature change.

```{r, fig.cap="Observational site temperature change across years"}
site_clim_tbl %>%
  ggplot(aes(year, tmp)) +
  geom_point() +
  geom_smooth(
    method = "lm", se = FALSE,
    color = "red", lty = "dashed"
  ) +
  ggpubr::stat_cor(aes(label = ..p.label..),
    p.accuracy = 0.05,
    color = "red"
  ) +
  facet_wrap(~site, scales = "free_y") +
  labs(
    x = "Year", y = "Mean annual temperature (°C)"
  )
```

Plot precipitation change.

```{r, fig.cap="Observational site precipitation change across years"}
site_clim_tbl %>%
  ggplot(aes(year, ppt)) +
  geom_point() +
  geom_smooth(
    method = "lm", se = FALSE,
    color = "red", lty = "dashed"
  ) +
  ggpubr::stat_cor(aes(label = ..p.label..),
    p.accuracy = 0.05,
    color = "red"
  ) +
  facet_wrap(~site, scales = "free_y") +
  labs(
    x = "Year", y = "Mean annual precipitation (mm)"
  )
```

Plot VPDmax change.

```{r, fig.cap="Observational site VPDmax change across years"}
site_clim_tbl %>%
  ggplot(aes(year, vpd)) +
  geom_point() +
  geom_smooth(
    method = "lm", se = FALSE,
    color = "red", lty = "dashed"
  ) +
  ggpubr::stat_cor(aes(label = ..p.label..),
    p.accuracy = 0.05,
    color = "red"
  ) +
  facet_wrap(~site, scales = "free_y") +
  labs(
    x = "Year", y = "Mean VPDmax (hPa)"
  )
```
